name: "codeql config"

queries:
  - uses: security-and-quality

paths:
  - ~/components

paths-ignore:
  - ~/coverage
  - ~/node_modules
  - ~/.npm
  - '**/*.test.js.snap'
  - '**/*.test.js'
  - '**/*.scss'