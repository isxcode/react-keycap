sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - master # build master branch only
script:
  - cd docs
  - hexo generate # generate static files
deploy:
  provider: pages
  skip-cleanup: true
  github-token: 77e5cfecd849c3d77f0414fc5dd71154b614a74e
  keep-history: true
  on:
    branch: master
  local-dir: /docs/public
